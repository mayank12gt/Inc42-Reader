<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="4dp"
    android:clickable="true"
    app:cardCornerRadius="8dp"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="2dp"
        android:background="?attr/colorSurfaceVariant">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"

            android:orientation="horizontal"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:paddingVertical="4dp"
            android:id="@+id/top_layout">

            <TextView
                android:id="@+id/feed_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
               android:layout_marginHorizontal="4dp"

                android:layout_gravity="center"
                android:ellipsize="end"
                android:includeFontPadding="false"
                android:maxLines="2"
                android:layout_weight="1"
                android:lineHeight="28dp"
                android:textAppearance="@style/TextAppearance24"
                android:fontFamily="@font/poppins_bold"
                android:textColor="?attr/colorPrimary"
                tools:text="Startup Stories" />


            <com.google.android.material.button.MaterialButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:backgroundTint="?attr/colorPrimary"
                android:text="Follow"
                android:layout_marginEnd="4dp"
                android:id="@+id/follow_btn"
                app:icon="@drawable/follow_icon"
                app:iconTint="@color/white"
                android:textColor="@color/white"
                app:iconGravity="textStart"
                app:cornerRadius="8dp"
                />

        </LinearLayout>



        <TextView
            android:id="@+id/feed_desc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"

            app:layout_constraintEnd_toEndOf="parent"

           android:layout_marginHorizontal="4dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:ellipsize="end"
            android:layout_marginVertical="2dp"
            android:includeFontPadding="false"
            android:textAppearance="@style/TextAppearance16"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/top_layout"
            tools:text="FinTech – Latest news, funding and startup financials, governments policies, sector landscape of FinTech in India" />



    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>